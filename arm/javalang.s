// Archivo generado automáticamente por el compilador JavaLang -> AArch64
// Aún en fase inicial: solo contiene un esqueleto de programa

.text
.global main

main:
    stp x29, x30, [sp, -16]!
    mov x29, sp

    // TODO: insertar instrucciones traducidas del programa JavaLang

    mov w0, #0
    ldp x29, x30, [sp], 16
    ret
